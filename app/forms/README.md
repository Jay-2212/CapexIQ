The 3-step Basic Mode wizard (Investment / Usage & Revenue / Operating Costs) plus the
Advanced Mode panel, and the pre-step (equipment + identity fields). Empty scaffold —
nothing built yet. See `wizard-state.md` in this folder for the full route map,
field-to-step assignment, and transition table — read that before building any
component here. See also SPEC.md §7, §10, §11 and `content/inputs-metadata.json` for
field-level detail.
